<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Appointments - My Hospital</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="/">🏥 My Hospital</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/appointment">Book Appointment</a></li>
          <li class="nav-item"><a class="nav-link active" href="/appointments">All Appointments</a></li>
          <li class="nav-item"><a class="nav-link" href="/auth/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Appointment List Section -->
  <div class="container my-5">
    <div class="card shadow-lg border-0 rounded-3">
      <div class="card-body p-4">
        <!-- <h2 class="text-center mb-4 text-primary">📋 All Appointments</h2>

        <% if (appointments.length === 0) { %>
          <div class="alert alert-info text-center">No appointments found.</div>
        <% } else { %>
          <div class="table-responsive">
            <table class="table table-bordered table-hover align-middle">
              <thead class="table-dark">
                <tr>
                  <th>Patient</th>
                  <th>Email</th>
                  <th>Department</th>
                  <th>Doctor</th>
                  <th>Date</th>
                </tr>
              </thead>
              <tbody>
                <% appointments.forEach(app => { %>
                  <tr>
                    <td><%= app.patientName %></td>
                    <td><%= app.patientEmail %></td>
                    <td><%= app.department %></td>
                    <td><%= app.doctor %></td>
                    <td><%= app.date.toDateString() %></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } %> -->

        <% if (typeof success !== "undefined" && success) { %>
  <script>
    alert("<%= success %>");
  </script>
<% } %>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-primary text-white text-center py-3 mt-5">
    <p class="mb-0">© <%= new Date().getFullYear() %> My Hospital | Admin Panel</p>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>